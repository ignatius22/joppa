// responsiveHelpers.js
export const rem = (px) => `${px / 16}rem`; // Converts px to rem based on a 16px root font size
export const vh = (percentage) => `${percentage}vh`; // Percentage of the viewport height
export const vw = (percentage) => `${percentage}vw`; // Percentage of the viewport width
